# ===========================================
# GitHub Actions Workflow: CI Build & Deploy
# Environment: Development
# Description:
#   - Builds and deploys the Anquilosaurios frontend app
#   - Runs on every push or pull request to 'development'
# ===========================================

name: CI Build & Deploy (development)

# -------------------------------------------
# Trigger: Run workflow on push or PR to 'development' branch
# -------------------------------------------
on:
  push:
    branches:
      - development
  pull_request:
    branches:
      - development

# -------------------------------------------
# Jobs Definition
# -------------------------------------------
jobs:
  # =========================================
  # Job: Build Stage
  # Purpose: Lint, type-check, and build the app
  # =========================================
  build:
    name: Build Stage for Anquilosaurios (development)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x]
    steps:
      # Step 1: Checkout source code from repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Set up the Node.js environment
      - name: Set up Node.js environment
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

      # Step 3: Cache npm dependencies to speed up builds
      - name: Cache npm dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: npm-${{ runner.os }}-package-lock.json
          restore-keys: |
            npm-${{ runner.os }}

      # Step 4: Install dependencies, lint the code, and run type checking
      - name: Install dependencies, lint & type check
        run: |
          npm ci
          npm run lint
          npm run check

      # Step 5: Build the application
      - name: Compile source code
        run: npm run build

      # Step 6: Upload build output as artifact
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: build

  # =========================================
  # Job: Deploy Stage
  # Purpose: Deploy the built app to staging
  # Depends on: build
  # =========================================
  deploy:
    if: github.event_name == 'push'
    name: Deploy Stage for Anquilosaurios (Staging)
    needs: build
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      # Step 1: Download the build artifacts from previous job
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: node-app
          path: build

      # Step 2: Authenticate with Azure using OIDC credentials stored in GitHub Secrets
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_C2193A217B9C42E5A793A7A059CE01F2 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_F09DB818996647A2B6D677AB0A8753FD }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_6E50FB326ECD4EA5A07F0602855DFA6B }}

      # Step 3: Deploy the downloaded build to the Azure Web App service
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'anquilosaurios-development-frontend'
          slot-name: 'Production'
          package: build
